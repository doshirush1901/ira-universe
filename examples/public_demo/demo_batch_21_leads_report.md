# Demo batch report (public)

PUBLIC DEMO DATA — fully synthetic. This file intentionally contains **no** embedded
`## \`folder\`` JSON blocks so `parse_batch_report` returns `{}` in public clones.
Add a private markdown report under `data/imports/` locally if you need extracted fields.
