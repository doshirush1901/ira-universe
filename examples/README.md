# Synthetic examples (public-safe)

All files here are **fabricated** for documentation and testing. They do **not** describe real customers, quotes, or inboxes.

| File | Purpose |
|------|---------|
| `sample_leads.csv` | Tiny CRM-style lead extract |
| `sample_customer_context.md` | Fake firmographic notes |
| `sample_email_thread.md` | Fake Gmail-style thread |
| `sample_quote_context.md` | Fake commercial context (**no real pricing**) |
| `sample_env.example` | Minimal env skeleton — copy ideas only; use root `.env.example` for full keys |

Replace these with **your own** data under private storage (never commit customer CRM exports, OAuth tokens, or mailbox dumps).
