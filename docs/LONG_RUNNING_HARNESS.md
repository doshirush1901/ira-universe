# Long-running harness (public note)

The **private** Ira stack supports adversarial validation and multi-phase harness runs (`ira harness review`, bounded task loops, human-in-the-loop send gates).

**ira-universe** does not include harness code or configs. Visitor mode is read-only documentation and ~12 MCP tools.

For harness operations, use the private operator repository (`docs/LONG_RUNNING_HARNESS.md` there).
